import './Coin.css';

const Coin = ({
    symbol,
    name,
    image,
    price,
    marketcap,
    volume,
    priceChange
}) => {
    return <>
        <h1>HELLO COIN</h1>
    </>
}

export default Coin;